apiVersion: v1
kind: Service
metadata:
  name: issue-service
  namespace: community-issues
spec:
  type: NodePort
  selector:
    app: issue-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30080
