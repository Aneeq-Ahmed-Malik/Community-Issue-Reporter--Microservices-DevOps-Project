apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: community-issues
spec:
  type: NodePort
  selector:
    app: auth-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30081
